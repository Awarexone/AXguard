"""Comment-only false positive bait — expect FALSE_POSITIVE or no VERIFIED."""

from __future__ import annotations

# False positive bait — comments only, no live call:
# execute(user_input)
# cursor.execute(f"SELECT {user}")
# requests.get(user_url)

PLACEHOLDER = True
