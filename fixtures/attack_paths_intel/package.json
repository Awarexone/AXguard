{
  "name": "attack-paths-intel-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture manifest for SBOM parsing (declared deps only, no CVE lookup).",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "1.4.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
