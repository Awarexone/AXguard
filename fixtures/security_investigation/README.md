# Security Investigation fixtures

Golden trajectories live in tests (`tests/test_security_investigation.py`).
`expected.json` lists conceptual case ids and allowed decision sets.

Investigations are **symbolic** — no network, no exploitation.
